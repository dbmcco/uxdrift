# Copy to .env and fill in real values.

# OpenAI-compatible API key used when running `uxrift run --llm`.
OPENAI_API_KEY=

# Optional overrides:
# UXRIFT_LLM_BASE_URL=https://api.openai.com/v1
# UXRIFT_LLM_MODEL=gpt-4o-mini

