[project]
name = "uxdrift"
version = "0.1.0"
description = "Playwright + LLM UX evaluation sidecar (Speedrift-adjacent)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "httpx>=0.27.0",
  "playwright>=1.58.0",
]
